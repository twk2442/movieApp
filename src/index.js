import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));
// App은 컴포넌트  리엑트는 컴포넌트로 동작한다
// component 는 html을 반환하는 함수
// <컴포넌트/> 같은 문법은 jsx라는 개념
